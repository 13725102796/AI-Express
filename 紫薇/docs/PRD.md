# 紫微灵犀 -- 紫微斗数 AI 解读平台 产品需求文档（PRD）

> 版本：v2.0
> 日期：2026-04-17
> 状态：修订稿（v1.0 → v2.0 重大调整：平台改为 uni-app 多端、商业模式改为积分制）

---

## 1. 产品概述

- **产品名称**：紫微灵犀
- **一句话定位**：基于紫微斗数排盘引擎与大语言模型（Gemini）的 AI 国学文化解读平台
- **目标平台**：微信小程序（主力）+ H5（引流），基于 uni-app 一套代码多端发布
- **目标用户**：对紫微斗数感兴趣、希望获得 AI 命理解读的 C 端用户（18-45 岁，移动端优先）
- **核心价值**：将传统紫微斗数排盘与 AI 深度解读结合，用户输入生辰信息后自动排盘，通过积分解锁不同维度的提示词模板获取个性化命理分析报告
- **合规定位**：传统文化知识服务 + AI 内容生成，所有解读内容标注"仅供娱乐参考"

### 1.1 产品背景

紫微斗数是中国传统命理学的核心体系之一，排盘流程专业、解读门槛高。当前市场上的同类产品（神机阁、文墨天机、紫微派等）多数仅提供排盘工具或固定模板的文字解读，AI 深度解读能力薄弱、个性化程度低。本产品利用已有的 FastAPI 排盘引擎（基于 iztro-py 计算库）和 Gemini 大模型，实现「排盘 + AI 深度解读」的闭环体验。

### 1.2 商业模式（三阶段演进）

| 阶段 | 策略 | 收入来源 | 所需资质 |
|------|------|---------|---------|
| **阶段一（MVP）** | 免费 + 积分体验 | 无直接收入（验证产品） | 个人主体微信小程序 |
| **阶段二（有用户后）** | 积分 + 广告变现 | 激励视频广告（看广告得积分） | 小程序日活 > 100 即可开通广告 |
| **阶段三（规模化）** | 订阅付费 + 广告 | 月度/年度订阅 + 广告收入 | 注册个体工商户，升级企业主体 |

### 1.3 现有技术资产

| 资产 | 说明 |
|------|------|
| 排盘 API | FastAPI 服务，已实现 `GET /paipan`（农历）、`GET /paipan/solar`（阳历）、`GET /paipan/json`（JSON 格式） |
| 计算引擎 | iztro-py v0.3.4，支持十二宫排列、主星/辅星/小星/神煞、大限/小限/流年计算 |
| 输出格式 | 树形文本格式 + 结构化 JSON（含 palaces、stars、decadal、ages 等完整数据） |

### 1.4 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | uni-app（Vue 3 + Vite） | 一套代码编译出微信小程序 + H5 |
| **UI 组件库** | uni-ui / uv-ui | 适配多端的组件库 |
| **状态管理** | Pinia | Vue 3 官方推荐 |
| **后端** | FastAPI（Python） | 复用现有排盘服务，扩展业务 API |
| **数据库** | PostgreSQL | 主数据存储 |
| **AI 模型** | Gemini 2.5 Pro | 命理解读生成 |
| **部署** | 待定 | 后端部署方案开发前确定 |

---

## 2. 用户画像

| 维度 | 主要用户 | 次要用户 |
|------|---------|---------|
| 角色 | 命理爱好者 | 专业命理师 |
| 年龄 | 22-35 岁 | 30-50 岁 |
| 痛点 | 看不懂排盘结果，不知如何解读十二宫星曜组合的含义 | 需要高效出具多维度分析报告给客户参考 |
| 期望 | 输入生辰即可获得通俗易懂、专业可信的命理分析 | 获取结构化分析框架，辅助自身解读 |
| 使用场景 | 晚间空闲时间，在手机上查看自己或朋友的运势 | 工作中批量查看客户命盘，快速获取分析要点 |
| 付费意愿 | 中等（愿意看广告换积分，或小额付费） | 较高（愿意订阅） |

---

## 3. 功能需求

### 3.1 用户注册与登录

- **描述**：用户通过手机号 + 密码注册/登录系统，首次注册自动赠送初始积分，登录后必须完善个人生辰信息才能使用排盘和 AI 解读功能
- **用户故事**：作为新用户，我希望快速注册并绑定我的生辰信息，以便系统为我生成紫微命盘
- **验收标准**：
  - AC-1: Given 用户首次访问，When 输入手机号和密码点击注册，Then 注册成功并自动登录，**系统赠送 100 积分**，跳转至生辰信息完善页
  - AC-2: Given 用户在生辰信息完善页，When 填写完出生日期（支持阳历/农历切换）、时辰（13 个选项：早子时至晚子时）、性别、出生地、是否闰月，Then 信息保存成功，跳转至首页
  - AC-3: Given 已注册用户再次访问，When 输入手机号和密码登录，Then 直接登录跳转首页
  - AC-4: Given 用户未完善生辰信息，When 尝试访问排盘或 AI 解读功能，Then 强制跳转至生辰信息完善页
  - AC-5: Given 用户已完善信息，When 进入「个人中心」，Then 可查看和修改生辰信息（修改后需重新排盘）
- **优先级**：P0
- **边界条件与异常场景**：
  - 手机号格式校验：仅接受中国大陆 11 位手机号，手机号唯一
  - 密码最少 8 位
  - 注册时手机号已存在则提示"该手机号已注册，请直接登录"
  - 阳历日期范围限制：1900-01-01 至当天
  - 时辰选择为必填项，不可留空
  - 出生地为选填项，精确到省/市级别
  - 农历闰月选项仅在选择农历输入时显示
  - 修改生辰信息后，历史排盘数据保留，新排盘覆盖当前命盘
  - 注册赠送积分仅限首次注册，同一手机号不可重复领取

### 3.2 紫微斗数排盘

- **描述**：系统根据用户绑定的生辰信息，调用现有排盘 API（`/paipan/json` 或 `/paipan/solar`）生成紫微命盘数据，并以可视化方式展示
- **用户故事**：作为已完善信息的用户，我希望系统自动为我排盘并清晰展示命盘结果，以便我了解自己的十二宫配置
- **验收标准**：
  - AC-1: Given 用户完善生辰信息后首次进入首页，When 系统检测到该用户尚无排盘数据，Then 自动调用排盘 API 并将结果存储至用户档案
  - AC-2: Given 排盘完成，When 用户进入「我的命盘」页面，Then 展示十二宫方位图，每宫显示：宫名、天干地支、主星（含亮度和四化标注）、辅星、大限年龄范围
  - AC-3: Given 用户点击某个宫位，When 宫位详情展开，Then 展示该宫完整信息：主星、辅星、小星、神煞（岁前星/将前星/十二长生/太岁煞禄）、小限、流年
  - AC-4: Given 用户修改了生辰信息，When 保存修改，Then 系统自动重新调用排盘 API 并更新存储数据
  - AC-5: Given 排盘 API 调用失败（网络超时/服务异常），When 发生错误，Then 展示友好错误提示并提供「重试」按钮
- **优先级**：P0
- **边界条件与异常场景**：
  - 排盘 API 超时时间设定为 10 秒，超时后提示用户重试
  - 排盘结果缓存于数据库，同一用户同一生辰信息不重复调用 API
  - 命盘数据包含基本信息（农历时间、四柱、五行局数、身主、命主、斗君、身宫）和十二宫详细数据
  - API 参数映射：阳历输入调用 `/paipan/solar`（date=YYYY-M-D 格式），农历输入调用 `/paipan/json`（year/month/day 分离参数 + is_leap_month）
  - time_index 范围 0-12，前端时辰选择器需正确映射

### 3.3 积分系统

- **描述**：积分是平台的核心虚拟货币，用户通过注册赠送、分享奖励、观看广告等方式获取积分，消耗积分解锁模板和使用 AI 解读
- **用户故事**：作为用户，我希望通过多种方式获取积分，以便解锁更多模板获取 AI 解读
- **验收标准**：
  - AC-1: Given 新用户注册成功，When 系统处理注册逻辑，Then 自动向用户账户充入 100 积分，并记录积分流水（类型：注册赠送）
  - AC-2: Given 用户在任意页面，When 查看积分余额，Then 顶部导航栏/个人中心始终显示当前积分余额
  - AC-3: Given 用户积分不足以解锁某模板，When 点击解锁按钮，Then 弹出提示"积分不足"，并展示获取积分的途径入口（分享得积分、看广告得积分）
  - AC-4: Given 用户成功分享小程序到微信聊天/群，When 分享回调确认，Then 奖励 10 积分，每日分享奖励上限 30 积分（3 次）
  - AC-5: Given 用户点击「看广告得积分」，When 完整观看一条激励视频广告，Then 奖励 20 积分，每日广告奖励上限 100 积分（5 次）
  - AC-6: Given 用户在个人中心点击「积分明细」，When 进入积分流水页，Then 按时间倒序展示所有积分变动记录（时间、类型、变动数量、余额）
- **优先级**：P0
- **积分经济设计**：

| 获取途径 | 单次积分 | 每日上限 | 阶段 |
|---------|---------|---------|------|
| 注册赠送 | 100 | 仅一次 | 阶段一 |
| 分享小程序 | 10 | 30（3次） | 阶段一 |
| 观看激励视频广告 | 20 | 100（5次） | 阶段二（日活>100后开通） |
| 连续签到 | 5-20（递增） | 一次/天 | 阶段一 |
| 邀请新用户注册 | 50 | 无上限 | 阶段一 |

| 消耗场景 | 消耗积分 | 说明 |
|---------|---------|------|
| 解锁免费模板 | 0 | 免费模板不消耗积分 |
| 解锁普通模板 | 30-50 | 单次解锁，永久可用 |
| 解锁高级模板 | 80-150 | 单次解锁，永久可用 |
| AI 解读（每次） | 10 | 每次使用已解锁模板进行 AI 解读时消耗 |

- **边界条件与异常场景**：
  - 积分不可提现、不可转让、不可交易
  - 积分无有效期（MVP 阶段不设过期机制）
  - 积分余额不可为负数，消耗前必须校验余额
  - 所有积分变动必须记录流水，支持审计
  - 分享奖励需验证真实分享行为（微信分享回调），防止刷积分
  - 广告奖励需验证广告完整播放（广告 SDK 回调确认）
  - 积分数值设计可后台动态配置，便于运营调整

### 3.4 模板商城

- **描述**：展示后台配置的各类命理解读模板，用户浏览、预览，使用积分解锁模板后方可用于 AI 解读
- **用户故事**：作为用户，我希望浏览不同维度的命理分析模板并按需解锁，以便有针对性地获取 AI 解读
- **验收标准**：
  - AC-1: Given 用户进入模板商城页，When 页面加载完成，Then 以卡片列表形式展示所有上架模板，每张卡片包含：模板名称、简介（50 字以内）、适用标签（如「事业」「感情」「流年」）、**所需积分**、已解锁人数
  - AC-2: Given 用户点击某个模板卡片，When 进入模板详情页，Then 展示：模板名称、详细说明（解读维度和内容概要）、示例解读截图/预览（脱敏处理）、**所需积分**、「积分解锁」按钮
  - AC-3: Given 用户点击「积分解锁」且积分充足，When 确认解锁，Then 扣除对应积分，模板状态变为「已解锁」，记录积分流水
  - AC-4: Given 用户点击「积分解锁」但积分不足，When 弹出提示，Then 展示"积分不足，还差 X 积分"，并提供获取积分途径入口
  - AC-5: Given 用户已解锁某模板，When 在商城浏览该模板，Then 卡片和详情页均显示「已解锁」标识，不重复扣费
  - AC-6: Given 模板列表超过 10 个，When 用户滚动页面，Then 支持下拉加载更多（分页加载，每页 10 个）
- **优先级**：P0
- **边界条件与异常场景**：
  - 模板积分为 0 时，视为免费模板，用户无需消耗积分直接解锁
  - 模板下架后，已解锁用户仍可使用；未解锁用户不可见
  - 单个模板解锁后永久有效（MVP 阶段不做有效期限制）
  - 积分扣除与模板解锁必须在同一事务中完成，避免扣积分但未解锁的情况

### 3.5 后台管理 -- 提示词模板 CRUD

- **描述**：管理员通过后台管理界面对提示词模板进行创建、编辑、上下架、积分定价管理
- **用户故事**：作为管理员，我希望灵活管理提示词模板库（增删改查、定价、上下架），以便控制商城展示内容和运营策略
- **验收标准**：
  - AC-1: Given 管理员登录后台，When 进入「模板管理」页面，Then 展示模板列表表格：ID、名称、标签、**所需积分**、状态（上架/下架）、解锁人数、创建时间、操作栏
  - AC-2: Given 管理员点击「新建模板」，When 填写模板信息（名称、简介、详细说明、提示词正文、标签、**所需积分**、示例解读预览），Then 模板创建成功，默认状态为「下架」
  - AC-3: Given 管理员编辑已上架模板的提示词正文，When 保存修改，Then 修改立即生效，已解锁用户下次使用该模板时将获得更新后的提示词解读
  - AC-4: Given 管理员将模板状态切换为「上架」，When 操作成功，Then 该模板在前端商城可见
  - AC-5: Given 管理员将模板状态切换为「下架」，When 操作成功，Then 该模板在前端商城不可见，已解锁用户的「已解锁」列表中仍保留
- **优先级**：P0
- **边界条件与异常场景**：
  - 提示词正文字段支持使用占位符变量：`{{排盘数据}}`、`{{用户性别}}`、`{{用户出生信息}}`，系统在调用 AI 时自动替换
  - 模板名称不可重复
  - 积分字段为非负整数，0 表示免费
  - 删除操作为软删除，已解锁记录保留
  - 后台管理须通过独立的管理员账号密码登录，与前端用户系统隔离

### 3.6 AI 智能解读

- **描述**：用户选择已解锁的提示词模板后，消耗积分，系统将模板提示词与用户排盘数据拼接，调用 Gemini API 生成个性化命理解读，支持流式输出
- **用户故事**：作为用户，我希望选择一个模板后立即获得基于我命盘数据的 AI 深度解读，以便了解自己在该维度的命理分析
- **验收标准**：
  - AC-1: Given 用户进入「AI 解读」页面，When 展示已解锁模板列表，Then 每个模板以卡片形式展示名称、标签和简介，卡片可点击选择
  - AC-2: Given 用户选择某个模板并点击「开始解读」，When 系统校验积分余额 >= 10（单次解读消耗），Then 扣除 10 积分，将模板提示词中的占位符替换为用户实际排盘数据（JSON 格式），拼接成完整 prompt 发送给 Gemini API
  - AC-3: Given Gemini API 返回流式响应，When 数据逐步到达，Then 前端以打字机效果逐字/逐段展示 AI 解读内容，页面自动向下滚动
  - AC-4: Given AI 解读完成，When 全部内容输出完毕，Then 展示「保存报告」和「分享」按钮，用户可将报告保存至「我的报告」
  - AC-5: Given 用户查看「我的报告」列表，When 点击某份历史报告，Then 完整展示该次 AI 解读内容（含使用的模板名称、生成时间）
  - AC-6: Given 同一用户使用同一模板重复解读，When 生成新报告，Then 新报告追加至历史记录，旧报告保留不覆盖
  - AC-7: Given 用户积分不足 10，When 点击「开始解读」，Then 提示"积分不足"并引导获取积分
- **优先级**：P0
- **边界条件与异常场景**：
  - Gemini API 调用超时上限 60 秒，超时后提示「解读生成超时，请重试」，**已扣除的积分原路退回**
  - Gemini API 返回内容为空或格式异常时，提示「解读生成失败，请稍后重试」并记录日志，**已扣除的积分原路退回**
  - 流式输出过程中用户关闭页面，已接收内容不保存（仅完整生成的报告才可保存），**积分不退回**（已消耗 AI 资源）
  - 流式输出过程中网络中断，展示「网络异常」提示和「重试」按钮
  - 单次 prompt 长度限制：提示词模板上限 2000 字符，排盘数据上限 5000 字符（JSON 格式）
  - AI 解读内容需在开头附加免责声明："以下内容由 AI 基于紫微斗数理论生成，仅供参考娱乐，不构成任何决策建议。"

### 3.7 个人中心

- **描述**：用户管理个人账户信息、积分、已解锁模板、历史报告的综合入口
- **用户故事**：作为用户，我希望在一个集中的页面管理我的账户、积分、已解锁内容和历史解读记录
- **验收标准**：
  - AC-1: Given 用户进入个人中心，When 页面加载完成，Then 展示以下分区：基本信息（头像、昵称、手机号）、**积分余额（突出显示）**、生辰信息摘要、快捷入口（已解锁模板数、历史报告数）
  - AC-2: Given 用户点击积分余额区域，When 进入积分中心页，Then 展示：当前余额、获取积分途径（分享、广告、签到）、积分明细流水
  - AC-3: Given 用户点击「我的模板」，When 进入已解锁模板列表，Then 展示所有已解锁的模板，点击可直接跳转至 AI 解读
  - AC-4: Given 用户点击「我的报告」，When 进入历史报告列表，Then 按时间倒序展示所有 AI 解读报告，每条显示模板名称、生成时间、摘要前 50 字
- **优先级**：P0（从 P1 提升，积分中心是核心功能）
- **边界条件与异常场景**：
  - 无已解锁模板时展示空状态引导："还没有解锁模板，去商城看看"
  - 无历史报告时展示空状态引导："还没有 AI 解读记录，选个模板开始吧"
  - 头像和昵称修改功能在 MVP 阶段不实现，展示默认头像和手机号后四位作为昵称

### 3.8 签到系统

- **描述**：用户每日签到获取积分，连续签到可获得递增奖励，提升用户留存
- **用户故事**：作为用户，我希望每天签到就能获得积分，鼓励我持续使用
- **验收标准**：
  - AC-1: Given 用户进入首页，When 今日未签到，Then 展示签到入口（悬浮按钮或横幅），点击后完成签到
  - AC-2: Given 用户点击签到，When 签到成功，Then 展示签到动画和获得积分数量，积分即时到账
  - AC-3: Given 用户连续签到 N 天，When 签到成功，Then 积分按连续天数递增（第1天5分，第2天8分，第3天10分，第4天12分，第5天15分，第6天18分，第7天20分，之后循环）
  - AC-4: Given 用户中断签到（某天未签到），When 次日签到，Then 连续天数重置为 1
- **优先级**：P0
- **边界条件与异常场景**：
  - 签到以自然日（北京时间 0:00 重置）为单位
  - 每日仅可签到一次，重复签到提示"今日已签到"
  - 签到记录和积分变动在同一事务中完成

### 3.9 分享裂变

- **描述**：用户分享小程序/报告至微信，获取积分奖励，同时实现用户增长裂变
- **用户故事**：作为用户，我希望分享有趣的解读给朋友，同时获得积分奖励
- **验收标准**：
  - AC-1: Given 用户在报告详情页点击「分享」，When 触发微信分享，Then 生成分享卡片（报告摘要前 50 字 + 模板名 + 产品 Logo），分享成功后奖励 10 积分
  - AC-2: Given 用户通过首页/个人中心点击「邀请好友」，When 生成专属邀请分享卡片并发送，Then 被邀请用户注册成功后，邀请人获得 50 积分
  - AC-3: Given 被邀请用户通过分享链接打开小程序，When 注册成功，Then 被邀请用户也获得注册赠送积分（100 积分）
- **优先级**：P1
- **边界条件与异常场景**：
  - 分享内容脱敏处理：隐藏用户手机号，生辰信息仅显示年份和性别
  - 分享积分奖励每日上限 30 积分（3 次），防止刷分
  - 邀请奖励需确认被邀请用户为新用户（手机号从未注册过）
  - H5 端分享生成链接（复制到剪贴板），小程序端使用原生分享能力

---

## 4. 非功能需求

### 4.1 性能要求

| 指标 | 要求 |
|------|------|
| 排盘 API 响应时间 | P99 < 2 秒 |
| 页面首屏加载时间 | < 3 秒（4G 网络） |
| AI 解读首 token 返回时间 | < 5 秒 |
| AI 解读流式输出速率 | 用户可感知的连续文字流，无明显卡顿 |
| 小程序包体积 | 主包 < 2MB，分包后总计 < 16MB |
| 并发支持 | MVP 阶段支持 100 并发用户 |

### 4.2 安全要求

| 维度 | 要求 |
|------|------|
| 用户认证 | 手机号+密码登录，JWT Token 机制，Token 有效期 7 天，支持续期 |
| 敏感数据存储 | 用户生辰信息、手机号等必须加密存储（AES-256 或同等强度） |
| 传输安全 | 全站 HTTPS，API 通信强制 TLS 1.2+ |
| Gemini API Key | 服务端环境变量存储，不暴露至前端 |
| 提示词保护 | 提示词模板正文仅存储于后端，前端不可见完整提示词内容 |
| 管理后台 | 独立域名或路径，IP 白名单限制（MVP 阶段至少做到独立登录） |
| 数据合规 | 用户注销时完整删除个人数据（生辰信息、报告内容） |
| 积分安全 | 积分变动必须服务端校验，前端不可直接修改积分；所有变动记录流水，防刷机制 |

### 4.3 兼容性要求

| 维度 | 要求 |
|------|------|
| 微信小程序 | 基础库 >= 2.20.0 |
| H5 浏览器 | Chrome 90+、Safari 14+、微信内置浏览器 |
| 设备 | 移动端优先（响应式设计），H5 端兼容桌面端 |
| 屏幕 | 最小适配宽度 375px（iPhone SE），最大适配宽度 1440px |

### 4.4 可用性要求

| 维度 | 要求 |
|------|------|
| 服务可用性 | 99.5%（MVP 阶段） |
| 错误处理 | 所有 API 调用失败场景均有友好提示和重试机制 |
| 离线体验 | 不支持离线使用，网络断开时展示统一提示页 |

---

## 5. 信息架构

### 5.1 页面结构

```
紫微灵犀
├── 启动页/引导页
├── 登录注册页（手机号+密码，注册即登录，赠送100积分）
├── 生辰信息完善页
│   ├── 出生日期选择（阳历/农历切换）
│   ├── 时辰选择
│   ├── 性别选择
│   ├── 出生地选择（选填）
│   └── 闰月选项（农历模式）
├── 首页（Tab 1）
│   ├── 签到入口
│   ├── 积分余额展示
│   ├── 命盘概览卡片
│   ├── 推荐模板入口
│   └── 最新报告入口
├── 我的命盘页
│   ├── 十二宫方位图
│   ├── 基本信息栏
│   └── 宫位详情弹层
├── 模板商城页（Tab 2）
│   ├── 模板卡片列表（显示所需积分）
│   └── 模板详情页
│       ├── 模板说明
│       ├── 示例预览
│       └── 积分解锁按钮
├── AI 解读页（Tab 3）
│   ├── 已解锁模板选择
│   ├── 解读中（流式输出）
│   └── 解读结果/报告详情
├── 个人中心页（Tab 4）
│   ├── 基本信息 + 积分余额
│   ├── 积分中心（获取途径 + 积分明细）
│   ├── 生辰信息管理
│   ├── 我的模板
│   ├── 我的报告
│   └── 邀请好友
└── 管理后台（独立入口，H5 页面）
    ├── 管理员登录
    ├── 模板管理（CRUD + 积分定价）
    ├── 用户管理（查看）
    ├── 积分配置（各途径积分数值调整）
    └── 数据概览（用户数、解读次数、积分消耗统计）
```

### 5.2 导航逻辑

- 前端采用底部 Tab 导航（4 个 Tab）：首页、模板商城、AI 解读、个人中心
- 登录/注册为独立全屏页面，登录状态下不可访问
- 生辰信息完善页为强制中间页，未完善时拦截所有核心功能入口
- 管理后台为独立 H5 页面（独立 URL 路径 `/admin`），不在小程序中暴露
- 积分不足时的弹层提供快捷跳转至积分获取途径

### 5.3 核心用户动线

**新用户首次使用动线**：
1. 打开小程序 -> 引导页 -> 登录注册页（输入手机号 + 密码）
2. 注册成功 -> **获得 100 积分** -> 强制跳转生辰信息完善页（填写出生信息）
3. 信息保存 -> 系统自动排盘 -> 跳转首页
4. 首页签到获得积分 -> 浏览推荐模板 -> 点击模板 -> 进入模板详情 -> **积分解锁**
5. 解锁成功 -> 跳转 AI 解读页 -> 选择模板 -> **消耗 10 积分开始解读** -> 流式查看结果
6. 解读完成 -> 保存报告 / **分享得积分**

**老用户复访动线**：
1. 打开小程序 -> 自动登录 -> 首页 -> **签到得积分**
2. 点击「AI 解读」Tab -> 选择已解锁模板 -> 消耗积分开始解读

**积分不足时的动线**：
1. 尝试解锁模板/开始解读 -> 提示积分不足
2. 跳转积分中心 -> 选择获取方式（签到/分享/看广告）
3. 获取足够积分 -> 返回继续操作

---

## 6. 数据模型

### 6.1 核心实体

#### User（用户）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| phone | string(11) | 是 | 手机号，加密存储，唯一索引 |
| password_hash | string(128) | 是 | 密码哈希值（bcrypt） |
| nickname | string(32) | 否 | 昵称，默认手机后四位 |
| avatar_url | string(256) | 否 | 头像 URL |
| points_balance | int | 是 | 当前积分余额，默认 0 |
| invite_code | string(8) | 是 | 用户专属邀请码，唯一 |
| invited_by | UUID | 否 | 邀请人用户 ID |
| created_at | datetime | 是 | 注册时间 |
| updated_at | datetime | 是 | 更新时间 |

#### UserProfile（用户生辰档案）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| user_id | UUID | 是 | 关联用户，唯一索引 |
| birth_type | enum | 是 | 日历类型：solar（阳历）/ lunar（农历） |
| birth_year | int | 是 | 出生年 |
| birth_month | int | 是 | 出生月 |
| birth_day | int | 是 | 出生日 |
| birth_time_index | int | 是 | 时辰索引 0-12 |
| gender | enum | 是 | 性别：male / female |
| is_leap_month | bool | 是 | 是否闰月（仅农历有效） |
| birth_place_province | string(32) | 否 | 出生地省 |
| birth_place_city | string(32) | 否 | 出生地市 |
| created_at | datetime | 是 | 创建时间 |
| updated_at | datetime | 是 | 更新时间 |

#### ChartData（排盘数据）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| user_id | UUID | 是 | 关联用户，唯一索引 |
| profile_id | UUID | 是 | 关联生辰档案 |
| chart_json | JSON | 是 | 排盘 API 返回的完整 JSON 数据 |
| chart_text | text | 是 | 排盘 API 返回的树形文本数据 |
| api_params | JSON | 是 | 调用排盘 API 时的请求参数（用于排重和审计） |
| created_at | datetime | 是 | 排盘时间 |

#### PointsTransaction（积分流水）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| user_id | UUID | 是 | 关联用户，索引 |
| type | enum | 是 | 类型：register_bonus / daily_checkin / share_reward / ad_reward / invite_reward / unlock_template / ai_reading / refund |
| amount | int | 是 | 变动数量（正数为收入，负数为支出） |
| balance_after | int | 是 | 变动后余额 |
| reference_id | UUID | 否 | 关联业务 ID（如模板 ID、报告 ID） |
| description | string(200) | 否 | 描述（如"解锁模板：事业运势分析"） |
| created_at | datetime | 是 | 创建时间 |

索引：(user_id, created_at DESC)

#### CheckinRecord（签到记录）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| user_id | UUID | 是 | 关联用户 |
| checkin_date | date | 是 | 签到日期 |
| consecutive_days | int | 是 | 连续签到天数 |
| points_earned | int | 是 | 本次签到获得积分 |
| created_at | datetime | 是 | 创建时间 |

复合唯一索引：(user_id, checkin_date)

#### PromptTemplate（提示词模板）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| name | string(64) | 是 | 模板名称，唯一 |
| description | string(200) | 是 | 简介（商城卡片展示） |
| detail | text | 是 | 详细说明（详情页展示） |
| prompt_content | text | 是 | 提示词正文，支持占位符 `{{排盘数据}}`、`{{用户性别}}`、`{{用户出生信息}}` |
| tags | JSON | 是 | 标签数组，如 ["事业","运势"] |
| points_cost | int | 是 | 解锁所需积分，0 表示免费 |
| preview_image_url | string(256) | 否 | 示例解读预览图 URL |
| status | enum | 是 | 状态：active（上架）/ inactive（下架）/ deleted（已删除） |
| unlock_count | int | 是 | 已解锁人数，默认 0 |
| sort_order | int | 是 | 排序权重，数字越小越靠前 |
| created_at | datetime | 是 | 创建时间 |
| updated_at | datetime | 是 | 更新时间 |

#### UserTemplate（用户已解锁模板）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| user_id | UUID | 是 | 关联用户 |
| template_id | UUID | 是 | 关联模板 |
| points_spent | int | 是 | 解锁时消耗的积分 |
| unlocked_at | datetime | 是 | 解锁时间 |

复合唯一索引：(user_id, template_id)

#### ReadingReport（AI 解读报告）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| user_id | UUID | 是 | 关联用户 |
| template_id | UUID | 是 | 使用的模板 |
| chart_id | UUID | 是 | 使用的排盘数据 |
| prompt_snapshot | text | 是 | 实际发送给 AI 的完整 prompt 快照 |
| ai_response | text | 是 | AI 返回的完整解读内容 |
| model_name | string(64) | 是 | 使用的模型名称（gemini-2.5-pro） |
| token_usage | JSON | 否 | Token 消耗统计 |
| points_spent | int | 是 | 本次解读消耗的积分 |
| share_token | string(32) | 否 | 分享链接 token |
| created_at | datetime | 是 | 生成时间 |

#### PointsConfig（积分配置，管理后台可调整）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| key | string(64) | 是 | 配置项名称，唯一 |
| value | int | 是 | 配置值 |
| description | string(200) | 否 | 说明 |
| updated_at | datetime | 是 | 更新时间 |

预设配置项：

| key | 默认值 | 说明 |
|-----|-------|------|
| register_bonus | 100 | 注册赠送积分 |
| checkin_day_1 | 5 | 连续签到第1天积分 |
| checkin_day_2 | 8 | 连续签到第2天积分 |
| checkin_day_3 | 10 | 连续签到第3天积分 |
| checkin_day_4 | 12 | 连续签到第4天积分 |
| checkin_day_5 | 15 | 连续签到第5天积分 |
| checkin_day_6 | 18 | 连续签到第6天积分 |
| checkin_day_7 | 20 | 连续签到第7天积分 |
| share_reward | 10 | 分享奖励积分 |
| share_daily_limit | 3 | 每日分享奖励次数上限 |
| ad_reward | 20 | 看广告奖励积分 |
| ad_daily_limit | 5 | 每日广告奖励次数上限 |
| invite_reward | 50 | 邀请新用户奖励 |
| reading_cost | 10 | 单次 AI 解读消耗积分 |

#### Admin（管理员）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| username | string(32) | 是 | 登录用户名，唯一 |
| password_hash | string(128) | 是 | 密码哈希值 |
| created_at | datetime | 是 | 创建时间 |

---

## 7. API 设计

### 7.1 已有 API（排盘引擎，保持不变）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/paipan` | 农历排盘，返回树形文本 |
| GET | `/paipan/solar` | 阳历排盘，返回树形文本 |
| GET | `/paipan/json` | 农历排盘，返回 JSON |

### 7.2 新增 API（业务服务）

#### 认证模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/v1/auth/register` | 手机号+密码注册（注册即登录，返回 Token，赠送积分） | P0 |
| POST | `/api/v1/auth/login` | 手机号+密码登录 | P0 |
| POST | `/api/v1/auth/token/refresh` | 刷新 JWT Token | P0 |

#### 用户模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| GET | `/api/v1/user/me` | 获取当前用户信息（含积分余额） | P0 |
| PUT | `/api/v1/user/profile` | 创建/更新生辰信息 | P0 |
| GET | `/api/v1/user/profile` | 获取生辰信息 | P0 |

#### 排盘模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/v1/chart/generate` | 触发排盘（内部调用已有排盘 API） | P0 |
| GET | `/api/v1/chart/me` | 获取当前用户的排盘数据 | P0 |

#### 积分模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| GET | `/api/v1/points/balance` | 获取当前积分余额 | P0 |
| GET | `/api/v1/points/transactions` | 获取积分流水列表（分页） | P0 |
| POST | `/api/v1/points/checkin` | 每日签到 | P0 |
| GET | `/api/v1/points/checkin/status` | 获取今日签到状态和连续天数 | P0 |
| POST | `/api/v1/points/share-reward` | 分享奖励积分（需验证分享行为） | P1 |
| POST | `/api/v1/points/ad-reward` | 广告观看奖励积分（需验证广告回调） | P1（阶段二） |
| POST | `/api/v1/points/invite-reward` | 邀请奖励积分（新用户注册时触发） | P1 |

#### 模板模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| GET | `/api/v1/templates` | 获取上架模板列表（分页，含所需积分） | P0 |
| GET | `/api/v1/templates/{id}` | 获取模板详情 | P0 |
| POST | `/api/v1/templates/{id}/unlock` | 积分解锁模板 | P0 |
| GET | `/api/v1/user/templates` | 获取用户已解锁模板列表 | P0 |

#### AI 解读模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/v1/reading/start` | 发起 AI 解读（扣积分 + 返回 SSE 流） | P0 |
| GET | `/api/v1/reading/reports` | 获取用户历史报告列表 | P0 |
| GET | `/api/v1/reading/reports/{id}` | 获取报告详情 | P0 |

#### 分享模块

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/v1/reading/reports/{id}/share` | 生成分享 token | P1 |
| GET | `/api/v1/share/{token}` | 分享链接公开访问（报告摘要） | P1 |

#### 管理后台 API

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/v1/admin/auth/login` | 管理员登录 | P0 |
| GET | `/api/v1/admin/templates` | 模板列表（含下架） | P0 |
| POST | `/api/v1/admin/templates` | 新建模板 | P0 |
| PUT | `/api/v1/admin/templates/{id}` | 编辑模板 | P0 |
| PATCH | `/api/v1/admin/templates/{id}/status` | 上下架切换 | P0 |
| DELETE | `/api/v1/admin/templates/{id}` | 软删除模板 | P0 |
| GET | `/api/v1/admin/users` | 用户列表（查看） | P1 |
| GET | `/api/v1/admin/points-config` | 获取积分配置 | P0 |
| PUT | `/api/v1/admin/points-config/{key}` | 修改积分配置 | P0 |
| GET | `/api/v1/admin/stats` | 数据概览（用户数、解读次数、积分统计） | P1 |

### 7.3 关键 API 交互流程

**积分解锁 + AI 解读流程**：
```
前端                          后端                         Gemini API
 |                             |                              |
 |-- POST /templates/{id}/unlock ->|                          |
 |   （积分解锁模板）           |                              |
 |                             |-- 1. 校验积分余额 >= 模板所需积分
 |                             |-- 2. 扣除积分，记录流水      |
 |                             |-- 3. 创建 UserTemplate 记录  |
 |<== 200: 解锁成功 ===========|                              |
 |                             |                              |
 |-- POST /reading/start ----->|                              |
 |   {template_id}             |                              |
 |                             |-- 1. 验证用户已解锁该模板    |
 |                             |-- 2. 校验积分余额 >= 10（解读消耗）
 |                             |-- 3. 扣除 10 积分，记录流水  |
 |                             |-- 4. 读取用户排盘数据        |
 |                             |-- 5. 拼接 prompt             |
 |                             |-- 6. 调用 Gemini Stream API ->|
 |<== SSE: data chunk 1 ======|<== stream chunk 1 ===========|
 |<== SSE: data chunk 2 ======|<== stream chunk 2 ===========|
 |<== SSE: ... ===============|<== ... ======================|
 |<== SSE: [DONE] ============|<== finish ====================|
 |                             |-- 7. 存储完整报告至 DB       |
 |                             |                              |
```

**异常情况积分退回**：
```
 |-- POST /reading/start ----->|                              |
 |                             |-- 扣除 10 积分               |
 |                             |-- 调用 Gemini API ---------> |
 |                             |<== 超时/错误 ================|
 |                             |-- 退回 10 积分，记录流水（type=refund）
 |<== 500: 解读失败，积分已退回|                              |
```

---

## 8. 多端适配策略

### 8.1 uni-app 条件编译策略

| 功能 | 微信小程序 | H5 |
|------|----------|-----|
| 登录方式 | 手机号+密码 | 手机号+密码 |
| 激励视频广告 | 原生 `<ad>` 组件 | 不展示（隐藏广告入口） |
| 分享 | 微信原生分享 API | 复制链接到剪贴板 |
| 签到 | 相同 | 相同 |
| 命盘展示 | 相同 | 相同 |
| 管理后台 | 不包含 | 独立 H5 路由 `/admin` |

### 8.2 小程序分包策略

```
├── pages/           （主包，< 2MB）
│   ├── index/       首页
│   ├── login/       登录注册
│   ├── profile-setup/  生辰信息完善
│   └── tabbar pages
├── sub-chart/       （分包：命盘相关）
│   ├── chart/       命盘展示
│   └── palace-detail/  宫位详情
├── sub-reading/     （分包：AI解读相关）
│   ├── reading/     AI 解读
│   └── report/      报告详情
└── sub-user/        （分包：个人中心）
    ├── points/      积分中心
    ├── templates/   我的模板
    └── reports/     我的报告
```

---

## 9. 设计指引

### 9.1 风格方向

- **整体调性**：东方美学 + 现代极简，传达「古典智慧，科技解读」的品牌感
- **色彩基调**：深紫色（#2D1B69）为主色调，金色（#D4A84B）为点缀色，背景色使用暖白（#FFF8F0）或深色模式（#1A1025）
- **字体选择**：标题使用带有书法韵味的中文字体（如思源宋体），正文使用现代无衬线体（如思源黑体/苹方）
- **图形元素**：适当使用星曜图标、天干地支符号、中式线条纹样作为装饰元素
- **命盘可视化**：传统正方形十二宫格排布（4x4 去四角，或标准 3x4 等排列），每宫位内分层显示信息
- **积分元素**：积分图标使用金色圆形（类似古钱币/星币），与东方美学主题统一

### 9.2 参考产品

| 产品 | 可参考的设计点 |
|------|---------------|
| 文墨天机 | 命盘排布样式、宫位信息层次 |
| 神机阁 | 模板商城的卡片展示 |
| 测测星座 | 社交分享卡片设计、流式输出的 AI 交互体验 |
| 小红书 | 内容卡片的视觉节奏和信息密度 |
| 多邻国 | 签到、积分、成就等游戏化激励设计 |

### 9.3 品牌调性关键词

- 专业可信
- 东方雅致
- 温暖亲近
- 科技赋能

---

## 10. 范围边界

### 10.1 MVP 包含（v1.0 -- 阶段一）

- 手机号 + 密码注册/登录
- **注册赠送积分（100 积分）**
- 生辰信息绑定和编辑（阳历/农历/时辰/性别/闰月）
- 自动排盘和十二宫可视化展示
- **积分系统（余额、流水、签到）**
- 模板商城（浏览、详情、**积分解锁**）
- AI 解读（Gemini API，SSE 流式输出，**每次消耗积分**）
- 历史报告查看
- **分享裂变（分享得积分、邀请得积分）**
- 管理后台（模板 CRUD + **积分配置**）
- 免责声明
- **uni-app 编译出微信小程序 + H5 双端**

### 10.2 阶段二（有用户后）

- 激励视频广告接入（看广告得积分）
- 广告收入统计和分析
- 管理后台数据看板

### 10.3 阶段三（规模化后）

- 注册个体工商户，升级企业主体
- 接入微信支付（积分充值/订阅付费）
- 月度/年度订阅制（解锁所有模板 + 每日 N 次免费解读）
- 支付宝 H5 支付（H5 端）

### 10.4 后续迭代（P2）

- 多命盘支持（为他人排盘）
- 流年/流月运势定期推送
- 命理师入驻（专业命理师上传自定义模板并分成）
- 用户评价和评分系统
- 多语言支持

### 10.5 明确排除

- 不做真人命理师在线咨询功能
- 不做八字、六爻、梅花易数等其他命理体系
- 不做社区/论坛功能
- 不做「合盘」（两人命盘匹配）功能
- 不做 Native App（iOS/Android）

---

## 11. 开放问题

### 已决策

| 编号 | 问题 | 决策 | 决策时间 |
|------|------|------|---------|
| Q1 | Gemini API 模型版本选择 | 使用 **gemini-2.5-pro** | 2026-04-17 |
| Q2 | 短信验证码服务商 | **MVP 阶段不用短信验证码**，采用手机号+密码注册/登录 | 2026-04-17 |
| Q5 | 阳历排盘缺少 JSON 接口 | **新增 `/paipan/solar/json` 接口** | 2026-04-17 |
| Q8 | 新业务 API 与排盘 API 部署方式 | **同一服务、同一项目**，扩展现有 FastAPI 服务 | 2026-04-17 |
| Q9 | 平台选择 | **uni-app 多端**，微信小程序（主力）+ H5（引流） | 2026-04-17 |
| Q10 | 商业模式 | **积分制**，三阶段演进：免费→广告→订阅 | 2026-04-17 |
| Q11 | 资质问题 | **阶段一个人主体**，阶段三注册个体工商户升级 | 2026-04-17 |
| Q12 | 收款方式 | **阶段一无收款**，阶段二广告收入，阶段三接入支付 | 2026-04-17 |

### 待决策

| 编号 | 问题 | 影响范围 | 建议决策时间 |
|------|------|---------|-------------|
| Q3 | 初始上线时预设哪些提示词模板？建议至少准备 2 个免费体验模板 + 3-5 个积分解锁模板 | 模板商城内容 | 开发中 |
| Q4 | 积分经济平衡：初始积分 100 是否足够体验核心流程？模板定价和解读消耗是否合理？ | 用户留存、转化率 | 内测阶段调整 |
| Q6 | 管理后台是否需要数据看板？ | 管理后台范围 | 阶段二 |
| Q7 | AI 解读内容的版权和法律风险评估 | 法务合规 | 上线前 |
| Q13 | 微信小程序审核类目选择：「生活服务>生活工具」还是「教育>文化教育」？ | 小程序上架 | 提审前 |
| Q14 | H5 端是否需要 ICP 备案？如需要则需提前准备个体工商户 | H5 上线 | 开发中 |

---

*文档结束*
