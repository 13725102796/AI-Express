# 排盘引擎（已有）
iztro-py>=0.3.4

# Web 框架
fastapi>=0.128.0
uvicorn[standard]>=0.39.0
python-multipart>=0.0.20

# 数据库（异步）
sqlalchemy[asyncio]>=2.0.36
# MySQL 驱动（async：asyncmy SA 官方推荐；同步回退：pymysql）
asyncmy>=0.2.9
pymysql>=1.1.0
alembic>=1.14.0

# 配置
pydantic>=2.10.0
pydantic-settings>=2.7.0

# 安全
PyJWT>=2.10.1
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1.0  # 锁定与 passlib 1.7 兼容（4.1+ / 5.x 会触发 72-bytes ValueError）
cryptography>=43.0.3

# LLM（Google 新统一 SDK）
google-genai>=1.0.0

# 工具
python-dateutil>=2.9.0
slowapi>=0.1.9

# 测试
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
